import { Text, View } from "react-native";
import React from "react";

const Homepage = () => {
  return (
    <View className="flex w-full h-screen justify-center items-center">
      <View className="bg-hero"></View>
    </View>
  );
};

export default Homepage;
